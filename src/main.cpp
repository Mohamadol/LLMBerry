#include "llmberry/llmberry.h"

#include <iostream>

int main(int argc, char* argv[]) {
    (void)argc;
    (void)argv;
    std::cout << "LLMBerry — A lightweight C++ runtime for LLM inference and decode optimization\n";
    std::cout << "Tensor abstraction (checkpoint 01) scaffold is ready.\n";
    return 0;
}
